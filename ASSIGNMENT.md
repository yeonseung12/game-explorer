과제 1. 게임 검색 탐색기 (순수 fetch)

## 배경
게임을 검색하고 둘러보는 화면을 `fetch`와 React 기본 훅만으로 만들어 봅니다. 로딩·에러 상태 관리, 요청 취소, 무한스크롤 같은 데이터 패칭의 기본기를 직접 다뤄 보는 것이 목표입니다. 데이터 패칭 라이브러리는 쓰지 않지만, URL 동기화는 React Router로 처리합니다.

## 과제
키워드로 게임을 검색하고, 정렬·장르 필터를 적용하며, 결과를 무한스크롤 그리드로 이어 보고, 마음에 드는 게임을 즐겨찾기할 수 있는 단일 페이지 앱을 만들어 주세요.

## 요구사항

### 필수 기능
- 검색·정렬·장르 필터를 한 화면에서 함께 제공하고, 셋을 하나의 요청으로 묶어 보냅니다.
- 검색 입력은 300~500ms 디바운스를 직접 구현해(`setTimeout`/`clearTimeout`) 매 글자마다 요청이 나가지 않게 합니다.
- 로딩·에러·빈 결과·성공 상태를 직접 관리하는 상태(`useState` 또는 `useReducer`)로 나눠 다루고, 각각 다른 화면을 보여 줍니다.
- **검색어·정렬·장르가 바뀌면 `page`를 1로 되돌리고 누적 결과를 비운 뒤 다시 채웁니다.** 새 검색 결과가 이전 목록 위에 쌓이지 않게 합니다.
- 무한스크롤은 IntersectionObserver로 다음 페이지를 불러오고, 받은 페이지를 직접 누적(append)합니다. 전체 개수·다음 페이지 유무로 종료를 판단합니다.
- **무한스크롤은 로딩 중 재요청을 막고(가드), 마지막 페이지에 도달하면 observer를 `disconnect`합니다. 같은 `page`를 두 번 호출하지 않습니다.**
- 검색어가 빠르게 바뀌면 이전 요청을 AbortController로 취소해, 늦게 도착한 응답이 최신 결과를 덮어쓰지 않게 합니다.
- **취소로 발생하는 `AbortError`는 에러 상태로 표시하지 않습니다(취소는 에러가 아닙니다). `catch`에서 `err.name === 'AbortError'`면 상태를 바꾸지 않고 무시합니다.**
- 검색어·정렬·장르를 URL에 동기화해, 새로고침·뒤로가기·링크 공유에도 같은 화면이 복원되게 합니다. **`page`는 URL에 동기화하지 않습니다.**
- 카드 이미지는 지연 로딩하고, 이미지 자리를 고정 비율(`aspect-ratio`)로 미리 잡아 레이아웃이 밀리지 않게(CLS 방지) 합니다. 이미지가 없는 게임은 대체 이미지로 처리합니다.
- 즐겨찾기는 누르는 즉시 화면에 반영하고 localStorage에 저장합니다.
- 데이터 패칭 로직(요청·로딩/에러 상태·취소)은 재사용 가능한 커스텀 훅(예: `useGamesSearch`)으로 정리합니다.

### 기술 제약
- React + TypeScript.
- 데이터 패칭 라이브러리(TanStack Query, SWR 등)를 쓰지 않고, `fetch`와 React 기본 훅(`useState`·`useEffect`·`useRef` 등)만으로 구현합니다.
- 라우팅·URL 동기화는 React Router 사용을 허용합니다(`useSearchParams`). 단, 데이터 패칭 자체는 여전히 `fetch`로 직접 다룹니다.
- API 키는 코드에 하드코딩하지 말고 환경 변수로 다룹니다.

### 데이터·API
- 게임 데이터는 RAWG API(`GET /api/games`)를 씁니다. 한 엔드포인트에 검색(`search`)·정렬(`ordering`, 예: `-rating`·`-released`로 내림차순)·장르(`genres`, 콤마로 구분한 ID·slug)·페이지네이션(`page`·`page_size`)을 함께 전달하고, 응답에는 전체 개수(`count`)·다음 페이지 유무(`next`)와 각 게임의 이미지(`background_image`)·평점(`rating`)·출시일(`released`)·장르(`genres`)가 들어 있습니다. 장르 목록은 `GET /api/genres`로 따로 받습니다.
- 즐겨찾기는 서버가 없으므로 localStorage에 저장합니다.
- 인증: RAWG는 키를 `?key=...` 쿼리 파라미터로 받습니다(헤더 아님). 클라이언트 앱에서는 키를 환경 변수로 빼도 번들과 네트워크 탭에 노출되므로, 환경 변수 분리는 *하드코딩 방지* 목적이지 비밀 유지가 아닙니다. RAWG 무료 키는 공개 키로 취급합니다.
- 제약: 무료 키를 발급받아 환경 변수로 씁니다. 개인·취미·소규모 프로젝트는 무료이며 출처(RAWG) 표기 조건이 있습니다. 무한스크롤 정도의 호출은 한도에 닿지 않습니다. (무료 키 정책의 정확한 한도·표기 조건은 발급 시점에 RAWG 문서에서 한 번 확인하세요.)

## 제출물
- 동작하는 배포 링크와 저장소.
- README: 로딩·에러 상태와 요청 취소·무한스크롤을 어떻게 다뤘는지, URL을 상태의 단일 소스로 두었는지 아니면 로컬 state와 동기화했는지와 그 근거, 더 시간이 있었다면 손보고 싶은 점.

## 평가 기준
- **상태 관리:** 로딩·에러·빈 결과·성공 전이를 직접 만든 상태로 정확히 다루고, 각각 다른 화면을 보여 주는지.
- **요청 취소:** 네트워크 탭을 Slow 3G로 스로틀한 상태에서 `abc`를 빠르게 입력·삭제했을 때, 늦게 도착한 응답이 최신 결과를 덮어쓰지 않는지. `AbortError`가 에러 화면으로 새지 않는지.
- **검색·필터 전환:** 검색어나 필터를 바꿨을 때 결과가 이전 목록 위에 섞이지 않고 새로 채워지는지(page 리셋·누적 초기화).
- **무한스크롤:** 페이지를 직접 누적하고 종료 조건을 정확히 처리하는지. 네트워크 탭에서 동일 `page` 파라미터가 중복 호출되지 않고(0회), 마지막 페이지 도달 후 추가 요청이 없는지.
- **이미지·CLS:** 지연 로딩과 자리 고정으로 초기 부담과 레이아웃 흔들림을 낮췄는지. Lighthouse 또는 Performance 패널에서 CLS ≤ 0.1.
- **캡슐화:** 데이터 패칭 로직이 재사용 가능한 커스텀 훅으로 깔끔히 분리됐는지.

## 학습 포인트
- **디바운스와 AbortController는 서로 다른 문제를 푼다.** 디바운스는 요청 *빈도*를, AbortController는 응답 *순서*를 다룹니다. 디바운스가 있어도 race condition은 사라지지 않습니다.
- **race가 실제로 터지는 순간:** ① 무한스크롤 다음 페이지 요청이 떠 있는 사이 검색어·필터를 바꿀 때, ② 디바운스 경과 후 연속된 검색 요청이 겹칠 때. 이 두 시나리오를 직접 재현해 보세요.
- **`AbortError` 구분:** 취소된 fetch는 reject되며 `AbortError`를 던집니다. 이를 에러 상태로 처리하면 타이핑할 때마다 에러 화면이 번쩍입니다. `err.name`으로 걸러 냅니다.
- **URL과 상태의 관계:** `useSearchParams`를 단일 소스로 두고 거기서 읽어 요청할지, 로컬 state와 양방향으로 동기화할지의 트레이드오프를 직접 판단하고 README에 근거를 남깁니다. `page`를 URL에서 제외한 이유도 함께 적으면 좋습니다.

## 소요 시간
권장 5~6시간.
